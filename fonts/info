Info:
I use 'Windows Compatible' nerd fonts because those were the only ones that worked out of the box for some reason.

Setup:
$ mkdir -p ~/.local/share/fonts
$ symlink ${DOTFILES}/fonts/nerdFonts ~/.local/share/fonts/nerdFonts
